const {
  setupShortcutHandlers,
  setupDialogHandlers,
  setupAPIHandlers,
  setupTestHandlers,
  setupAllHandlers
} = require('./handlers.js');

module.exports = {
  setupShortcutHandlers,
  setupDialogHandlers,
  setupAPIHandlers,
  setupTestHandlers,
  setupAllHandlers
};
